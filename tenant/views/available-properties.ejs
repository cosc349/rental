<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Available Properties</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="dashboard-container">
        <header>
            <h1>Available Properties</h1>
            <a href="/dashboard" class="back-btn">Back to Dashboard</a>
        </header>
        <main>
            <div class="property-grid">
                <% if (properties.length > 0) { %>
                    <% properties.forEach(property => { %>
                        <div class="property-card">
                            <h3><%= property.property_address %></h3>
                            <p><strong>Rental Price:</strong> $<%= property.rental_price %></p>
                            <p><strong>Bedrooms:</strong> <%= property.bedrooms %></p>
                            <p><strong>Bathrooms:</strong> <%= property.bathrooms %></p>
                            <p><strong>Current Tenants:</strong> <%= property.tenant_count %></p>
                            <p><strong>Available Spots:</strong> <%= property.bedrooms - property.tenant_count %></p>
                            <form action="/rent-property" method="POST">
                                <input type="hidden" name="property_id" value="<%= property.property_id %>">
                                <button type="submit" class="rent-btn">Rent This Property</button>
                            </form>
                        </div>
                    <% }); %>
                <% } else { %>
                    <p>No properties are available for rent at the moment.</p>
                <% } %>
            </div>
        </main>
    </div>
</body>
</html>